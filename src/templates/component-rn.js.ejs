import React, { Component } from 'react';
import { Text, View } from 'react-native';

export default class <%= props.name %> extends Component {
    render() {
        return (
            <View>
                <Text><%= props.name %> is working!</Text>
            </View>
        )
    }
}
